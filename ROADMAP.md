# Roadmap

## What can be improved if givent more time?

```
Frontend:
1) Full document display with pagination as the BE API has pagination set up already.
2) Refactor UI components to be tablet and mobile view.
- Hide side bar and profile into a burger menu
- Streamline Career Goals and Documents component into mobile view
3) Login credentials to have password
4) Cloudflare protection from DDOS
5) Load balancer if appliation has more users
```

```
Backend:
1) Add dynanmoDB
2) Login credentials to be verified with dynamoDB
3) Get Career Goals and Documents data from dynamoDB instead of sample data
4) Load balancer if appliation has more users
```
